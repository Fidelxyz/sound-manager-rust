{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "fs:allow-exists",
    "fs:allow-create",
    "fs:allow-write",
    "store:allow-load",
    "store:allow-get",
    "store:allow-set"
  ]
}
